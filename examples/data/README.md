# Example data

Datasets used by the notebooks in `examples/`.

Keep files here small (a few MB at most) — this repo is public and git retains
every version of a binary forever. If a source dataset is large, commit a sampled
subset rather than the full file.

## Sources

| File | Source | Licence |
|---|---|---|
| _(add each dataset here)_ | | |
